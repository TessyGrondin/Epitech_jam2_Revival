ordre de priorité :

map_collisions
    si position du bg.x == 1920 / 2 gauche = impossible
    si position du bg.y == 1080 / 2 up = impossible
    si position du bg.x == 1920 / 2 + 1920 * 3 = droite impossible
    si position du bg.y == 1080 / 2 + 1080 * 3 = bas impossible
menu
    asset menu
    boutons
        démarrer
        quitter
dialogues
    après avoir appuyer sur démarrer
    une textbox
    un array de string
    un sprite/spritesheet
    mise en context
    win dialogue
    lose dialogue

sound design